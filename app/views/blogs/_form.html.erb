<%= form_for @blog do |f| %>

<%= f.label :title%>
<%= f.text_field :title%>

<%= f.label :content%>
<%= f.text_area :content%>

<%= f.file_field :main_photo %>

<%= select_tag 'category_id', options_for_select(Category.all.collect{ |c| [c.category] }) %>
<%
=begin%>
 <%=  f.collection_select( :blog, :category_id, Category.all, :id, Category.Category ) %> 
<%
=end%>
<%= @blog.errors.full_messages %>

<%= f.submit %>

<%end %>