<%= form_for @blog do |f| %>

<%= f.label :title%>
<%= f.text_field :title%>

<%= f.label :content%>
<%= f.text_area :content%>

<%= f.file_field :main_photo %>

<%= f.select :category_id, Category.all.map {|c| c.category} %>

<%= f.submit %>

<%end %>